shortcut:
 shortcut1:
  auth:
   api_token: "${{ secrets.shortcut-api2 }}"
  preferences:
   project_id: 2
jira:
 my-jira:
  auth:
   api_token: "${{ secrets.jira-api }}"
   email: "my-mail@jira.com"
