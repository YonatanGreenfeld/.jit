shortcut:
 shortcut1:
  auth:
   api_token: "${{ secrets.shortcut-api2 }}"
  preferences:
   project_id: 2
jira:
  shimi:
    auth:
      api_token: ${{ secrets.jira-api }}
      email: eliya@jit.io
      domain: eliya-test-99
    preferences:
      project_id: 10001
      issue_type_id: 10001
      fields:
        customfield_10034:
          - value: "1"
